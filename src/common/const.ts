/**
 * 座位类型
 */
export const enum SiteGroupEnum {
  'DifferentSex' = 'different_sex', // 异性同桌
  'SameSex' = 'same_sex', // 同性同桌
  'RandomSex' = 'random_sex', // 随机同桌
}

export const enum Sex {
  'Male' = 'male',
  'Female' = 'female',
}
